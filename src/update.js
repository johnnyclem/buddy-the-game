// Game logic — called once per frame during 'play' mode
// dt: delta time in seconds (fixed at 1/60)
function update(dt) {
  state.tick++;

  // TODO: update player, enemies, physics, collisions, etc.
}
